name: Auto Update Sitemap
on:
  schedule:
    - cron: '0 2 * * *' # Каждую ночь в 2:00
  workflow_dispatch: # Кнопка ручного запуска

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests curl_cffi

      - name: Run script
        run: python purify.py

      - name: Commit and Push changes
        run: |
          git config --global user.name 'SitemapBot'
          git config --global user.email 'bot@github.com'
          
          # Просто ищем все файлы sitemap_part в корне
          if ls sitemap_part*.xml 1> /dev/null 2>&1; then
            git add sitemap_part*.xml
            git commit -m "Sitemap Auto-Update $(date)" || echo "No changes to commit"
            git push origin main
          else
            echo "Критическая ошибка: файлы sitemap_part*.xml не найдены!"
            exit 1
